---
- name: Configure Dev Environment Servers
  hosts: dev
  become: yes
  vars:
    env: dev
    timezone: America/New_York
  
  roles:
    - common
    - webserver
  
  vars_files:
    - "vars/{{ environment }}/main.yml"