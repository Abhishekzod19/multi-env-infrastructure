---
- name: Create app directory
  file:
    path: /opt/app
    state: directory
    mode: '0755'

- name: Create sample app
  copy:
    content: |
      #!/bin/bash
      echo "App server is running"
    dest: /opt/app/hello.sh
    mode: '0755'
